package matematica;

public class Areas {
	int calculararea(int x, int y) {
		return (x*y) / 2;
		
	}
	
	double calculararea(double x, double y) {
		
		return(x*y);
	}
	
	int calculararea(int x) {
		return (x*x);
	}
}
